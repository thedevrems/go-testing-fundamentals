package tri

import "testing"

// TestVide doit vérifier le comportement de la fonction tri lorsqu'elle reçoit
// un tableau vide ou nil en entrée. Elle s'assure que tri() retourne également
// un tableau vide dans ces cas-là.
func TestVide(t *testing.T) {
	// A Remplir
}

// TestNonModif doit vérifier que la fonction tri ne modifie pas l'entrée fournie.
// Elle assure que la taille du tableau d'entrée reste inchangée et que son contenu
// ne soit pas modifié avant l'appel à tri.
func TestNonModif(t *testing.T) {
	// A Remplir
}

// TestTri vérifie que la fonction tri trie correctement un tableau en entrée.
// Elle compare le tableau retourné avec le tableau trié attendu.
func TestTri(t *testing.T) {
	// A Remplir
}
